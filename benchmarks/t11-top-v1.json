{
  "fixture": "t11-top-v1",
  "interval_ms": 1000,
  "slow_interval_ms": 5000,
  "samples": 12,
  "warmup_samples": 2,
  "top_limit": 10,
  "top_sort": "cpu",
  "top_probe_cap": 4096,
  "top_collection_budget_ms": 100,
  "max_steady_cpu_percent_one_core": 5.0,
  "max_resident_bytes": 67108864,
  "max_steady_collection_ms": 100,
  "max_top_collection_ms": 150,
  "max_first_frame_ms": 150,
  "max_shutdown_ms": 1000,
  "scope": "Local macOS native status with explicit --top 10 cpu ranking. NDJSON and terminal modes measured separately; first two samples excluded from steady-state checks. Process names/PIDs are sensitive local data and are never written to benchmark artifacts."
}
